the most common design patterns that occur in modern JavaScript code. 

The patterns we’ll be covering in this series are:
- Plain Old JavaScript Objects and Object Constructors
- Factory Functions and the Module Pattern
- Classes
- ES6 Modules




1.  Objects and Object Constructors

===> object basics - https://www.theodinproject.com/lessons/foundations-object-basics

=> // dot notation
myObject.property; // 'Value!'
// bracket notation
myObject["obnoxious property"]; // [Function]

both are used and dot noation is cleaner but sometime dot notation not work like property is string then obj."abc kk" - not works.and also we can't use variable in dot notation. like variable name is var1 then obj.var1 not works. (use obj[var1])


=> create object constructor :
function Player(name, marker) {
  this.name = name;
  this.marker = marker;
  this.sayName = function() {
    console.log(this.name)
  };
}
const player1 = new Player('steve', 'X');
const player2 = new Player('also steve', 'O');
player1.sayName(); // logs 'steve'
player2.sayName(); // logs 'also steve'

Note: It is almost always best to return things rather than putting console.log() directly into the function. In this case, return the info string and log it after the function has been called:
            Example : console.log(theHobbit.info());



===> Prototype : All objects in JavaScript have a prototype. The prototype is another object that the original object inherits from, which is to say, the original object has access to all of its prototype’s methods and properties.
=>importance : it is shared object. Defining every property and function takes up a lot of memory, especially if you have a lot of common properties and functions, and a lot of created objects!


All objects in JavaScript have a prototype. prototype is anothe object.that the original object inherits from, and has access to all of its prototype’s methods and properties

=> Accessing an object’s prototype
Object.getPrototypeOf(player1) === Player.prototype; // returns true
Object.getPrototypeOf(player2) === Player.prototype; // returns true

=> define new method(injecting properties) in prototype of particular object :
Player.prototype.sayHello = function() {
   console.log("Hello, I'm a player!");
};

player1.sayHello(); // logs "Hello, I'm a player!"
player2.sayHello(); // logs "Hello, I'm a player!"



==> new Keyword : one importance of new Keyword is after giving new properties from yourself in prototype, if you create new objects using that constructor your prototype's properties not injected. so We say browser that we injected some properties in protype by new Keyword.
/*  chai - notes

Here's what happens behind the scenes when the new keyword is used:

A new object is created: The new keyword initiates the creation of a new JavaScript object.

A prototype is linked: The newly created object gets linked to the prototype property of the constructor function. This means that it has access to properties and methods defined on the constructor's prototype.

The constructor is called: The constructor function is called with the specified arguments and this is bound to the newly created object. If no explicit return value is specified from the constructor, JavaScript assumes this, the newly created object, to be the intended return value.

The new object is returned: After the constructor function has been called, if it doesn't return a non-primitive value (object, array, function, etc.), the newly created object is returned.

*/


